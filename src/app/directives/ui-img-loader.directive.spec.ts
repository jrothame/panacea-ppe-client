import { UiImgLoaderDirective } from './ui-img-loader.directive';

describe('UiImgLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new UiImgLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
